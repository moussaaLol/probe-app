{
  "name": "probe-app",
  "version": "1.0.0",
  "description": "Probe App - App Store",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Building function assets...' && exit 0",
    "start": "vercel start"
  },
  "dependencies": {
    "stripe": "^12.0.0",
    "firebase-admin": "^11.11.1" 
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "engines": {
    "node": ">=16.x"
  }
}
